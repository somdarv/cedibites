// Re-export everything from unified constants
// Modules that still import from here will continue to work
export {
    STATUS_CONFIG,
    SOURCE_ICON,
    SOURCE_LABEL,
    PAYMENT_LABELS,
    KANBAN_COLUMNS,
    KANBAN_COLUMNS as COLUMNS,
} from '@/lib/constants/order.constants';
